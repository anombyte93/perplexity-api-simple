# ========================================
# Perplexity API Simple - Configuration
# ========================================

# Perplexity Cookie (REQUIRED for authenticated searches)
# Get this from https://www.perplexity.ai
# 1. Login to Perplexity
# 2. Open DevTools (F12)
# 3. Go to Application/Storage → Cookies
# 4. Copy the entire cookie string (all cookie fields semicolon-separated)
PERPLEXITY_COOKIE=

# Server Configuration
# Port to expose the API server (default: 8765)
PORT=8765

# ========================================
# Docker Deployment Notes
# ========================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Add your PERPLEXITY_COOKIE value
# 3. Run: docker-compose up -d
# 4. Access dashboard at: http://localhost:8765
# 5. Generate API keys via the web dashboard
#
# For AWS/Cloud deployment:
# - Ensure port 8765 is open in security groups
# - Use environment variables or secrets manager for PERPLEXITY_COOKIE
# - Mount volumes for persistent API keys: .api_keys.json
# ========================================
